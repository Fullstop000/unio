name: python-ci

on:
  push:
    branches: [master]
    paths:
      - "python/**"
      - "docs/contract.json"
      - ".github/workflows/python-ci.yml"
  pull_request:
    branches: [master]
    paths:
      - "python/**"
      - "docs/contract.json"
      - ".github/workflows/python-ci.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: python/pyproject.toml
      - run: python -m pip install -e './python[dev]'
      - if: matrix.python-version == '3.11'
        run: ruff format --check python
      - if: matrix.python-version == '3.11'
        run: ruff check python
      - if: matrix.python-version == '3.11'
        working-directory: python
        run: pyright
      - run: pytest python/tests

  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install build twine
      - working-directory: python
        run: python -m build
      - working-directory: python
        run: twine check dist/*
      - name: Read package version
        run: echo "PACKAGE_VERSION=$(python -c 'import tomllib; print(tomllib.load(open("python/pyproject.toml", "rb"))["project"]["version"])')" >> "$GITHUB_ENV"
      - run: python -m pip install --force-reinstall --no-deps python/dist/*.whl
      - run: python -c "import os, unio; from unio import Agent, AgentError, Session, Stream; assert unio.__version__ == os.environ['PACKAGE_VERSION']"
      - run: python -m pip install --force-reinstall --no-deps python/dist/*.tar.gz
      - run: python -c "import os, unio; assert unio.__version__ == os.environ['PACKAGE_VERSION']"
